/* ============================
LEVEL 1 : [ Problem #6 - Format Issue ]
1. Using variables `x` and `y`, calculate their sum.
2. For proper formatting, use template literals or `${}` (instead of concatenation) so that the
   output correctly displays the values of `x`, `y`, and their sum.
============================ */
let x = 2
let y = 3
let sum = 0

// your code goes here

//assigns total of x and y to sum
sum = x + y;

// prints information
console.log(`The sum of ${x} and ${y} is ${sum}.`)