/* ============================
LEVEL 3 : [ Problem #20 - Rotate Array ]
Define a function that takes an array and a number k, and rotates the array to the right by k positions.
============================ */
function rotateArray(arr, k) {
    // your code goes here
}

// TEST CASES
console.log(rotateArray([1, 2, 3, 4], 1));    // Output: [4, 1, 2, 3]
console.log(rotateArray([1, 2, 3], 0));       // Output: [1, 2, 3]
console.log(rotateArray([], 5));              // Output: []